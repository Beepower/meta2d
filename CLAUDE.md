# 项目 AI 协作规则

## 写作与写代码准则

### 品质基线：清晰

清晰是所有规则的共同基线——代码、注释、文档、给用户的回复，全部适用。

**判断标准只有一个：** 一个没有参与本次对话的人，能不能不问任何问题就读懂？读不懂，就重写成读得懂的——用大白话保底，用信雅达提质。

"信雅达"在这里指：意思准确（信）、表达克制不啰嗦（雅）、读者能接住（达）。不要窄化为"翻译时才用"。

写作时按 `docs/rules/AI-writing.md` 的「写作四条准则」扫一遍：

- **[动手前] 想清楚**——解决什么、给谁看、读完做什么；三问答不出别动笔
- **[动手前] 组织好**——动笔前列骨架，按读者认知路径排章节
- **[动手时] 写直白**——每段先给结论；平实保底，信雅达提质
- **[完成后] 打磨干净**——对照前三条做四个尺度复查（主题/结构/段落/字面）

写作四条和六条行为准则共用骨架（动手前 / 动手时 / 完成后）——AI 的本能是跳过思考直接产出，所有规则用强制动作对抗这个本能。

**中文表达：** 讲道理、做判断用中文动词和连接词。英文只留给代码标识符、行业标准缩写、首次出现的术语括注。

### 写代码的六条准则

写代码分三个时刻：动手前 / 动手时 / 完成后。

**[动手前]**
- **P1 题意已明** —— 把会改变实现选择的假设说出来，请用户裁决；不要替我做业务决定
- **P2 旧码已通** —— 读懂隐式约定和上下游；你不懂的代码不要改
- **P3 标尺已立** —— 用可二元判断的条件定义"做完了"，定义先于实现

**[动手时]**
- **P4 行行有据** —— 拒绝顺手优化，拒绝无要求抽象；每行改动都要能指向需求条目
- **P5 临疑能止** —— 遇到新歧义、需求冲突、验收错误、盲区代码，立即停下当前实现，主动报告状态并请示

**[完成后]**
- **P6 取舍尽明** —— 交代放弃了哪些方案、发现但没修哪些问题；用户已知的不必复述

每条准则的子条款、边界、上限、反模式、反例在 `docs/rules/AI-writing.md`。

以下情况主动读取该文档：

- 任务涉及修改 200 行以上的既有代码 → 读 P2、P4 完整版
- 任务涉及新增独立模块或独立章节 → 读 P4 反模式（事后追认）完整版
- 任务包含"风格修订 / 重构 / 整理"等模糊动词 → 读 P3 完整版
- 任务过程中触发 P5 停顿 → 读 P5 四类停顿条件完整版
- 任务是写超过 500 字的独立文档（设计稿、评审意见、说明书等）→ 读写作四条完整版（特别是第二条·组织好的列骨架，第四条·打磨干净的四个尺度复查）
- 用户明确说"按规则做"或"按 P 系列做" → 读完整文档