# 11d — 卫星包 Meta2d / canvas 调用点详细列表

> Source: `packages/{flow,form,fta,chart,svg}-diagram/src/` — 5 个 adopt 卫星包
> Generated by: `docs/refactor-public-api/11-scan-scripts/callsites.ts`
> Generated at: 2026-05-02T09:47:44.336Z
> SoT: `canvas-apis-enriched.json` + `core-apis-enriched.json`(Δ1.2 + Δ2.3)
> 调用点形态:仅列 D-P0-13 receiver pattern 命中(`canvas.X` / `meta2d.X|m2d.X|engine.X`),不含 destructure / property-access-only / 暗线 sub-form 已记 11h G-001

## 总览

- 命中 file 数:6
- 总 call site 数:12
- 命中 method 数(去重):7

### packages/form-diagram/src/table2.ts

| # | 行 | 调用方法 | 来源 | 调用点(snippet)|
|---|----|---------|------|------------------|
| D001 | 80 | `mousePos` | canvas.ts | `let mousePos = pen.calculative.canvas.mousePos;` |
| D002 | 96 | `mousePos` | canvas.ts | `const { x, y } = pen.calculative.canvas.mousePos;` |
| D003 | 682 | `showInput` | canvas.ts | `pen.calculative.canvas.showInput(pen, rect, '#ffffff');` |
| D004 | 915 | `delForce` | canvas.ts | `pen.calculative.canvas.delForce(pen.calculative.canvas.findOne(child));` |
| D005 | 1049 | `updatePenRect` | canvas.ts | `pen.calculative.canvas.updatePenRect(_pen);` |

### packages/chart-diagram/src/echarts.ts

| # | 行 | 调用方法 | 来源 | 调用点(snippet)|
|---|----|---------|------|------------------|
| D006 | 136 | `externalElements` | canvas.ts | `pen.calculative.canvas.externalElements?.parentElement.appendChild(div);` |
| D007 | 882 | `setValue` | core.ts | `meta2d.setValue({ id: pen.id, echarts }, { render: false, doEvent: false });` |

### packages/chart-diagram/src/LightningChart.ts

| # | 行 | 调用方法 | 来源 | 调用点(snippet)|
|---|----|---------|------|------------------|
| D008 | 55 | `externalElements` | canvas.ts | `pen.calculative.canvas.externalElements &&` |
| D009 | 56 | `externalElements` | canvas.ts | `pen.calculative.canvas.externalElements.parentElement.appendChild(div);` |

### packages/form-diagram/src/slider.ts

| # | 行 | 调用方法 | 来源 | 调用点(snippet)|
|---|----|---------|------|------------------|
| D010 | 170 | `mouseDown` | canvas.ts | `if (pen.calculative.canvas.mouseDown) {` |

### packages/form-diagram/src/table.ts

| # | 行 | 调用方法 | 来源 | 调用点(snippet)|
|---|----|---------|------|------------------|
| D011 | 289 | `showInput` | canvas.ts | `pen.calculative.canvas.showInput(pen, rect, '#ffffff');` |

### packages/chart-diagram/src/highcharts.ts

| # | 行 | 调用方法 | 来源 | 调用点(snippet)|
|---|----|---------|------|------------------|
| D012 | 67 | `externalElements` | canvas.ts | `pen.calculative.canvas.externalElements?.parentElement.appendChild(div);` |


## Δ3.3 valuable discoveries(卫星端)

### 卫星调用集中在 canvas 暗线(G-001 sub-form)

- canvas method calls:**11**(92%)— 全部通过 `pen.calculative.canvas.X` chain(G-001 6 sub-form)
- Meta2d facade calls:**1**(8%)— 通过 `canvas.parent` 反向访问 Meta2d 实例(G-001 sub-B)
- 工程含义:**卫星几乎不直接 import @meta2d/core**,全部通过 Pen 实例 chain 暗线访问。P4 拆 canvas 时 sat call site 全部受 G-001 暗线裁决方向影响

### V2 vs 卫星 surface consumption 不对称(P3 切换风险定位)

| 维度 | V2 | 卫星 | 比例 |
|---|---|---|---|
| call sites | 195 | 12 | V2 16x |
| 命中文件 | 15 | 6 | V2 NaNx |
| canvas method | 151 | 11 | V2 14x |
| Meta2d facade | 44 | 1 | V2 44x |

**P3 切换 risk 定位**:V2 adapter 是 P3 切换主战场(195 call sites);卫星 12 sites 走暗线 G-001,P3 + P7 共同处置(暗线裁决 + 卫星包重写)
